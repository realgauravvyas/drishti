# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# --- OS / editors ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- DRISHTI generated artefacts -------------------------------------------
# Raw Overpass responses: large, and refetchable with fetch_osm.py
drishti/backend/data/_raw_*.json
drishti/backend/data/_fetch.log
drishti/backend/data/_server.log

# Derived caches, rebuilt by run_all.py in ~50s
drishti/backend/data/roadgraph.pkl
drishti/backend/data/livecache.json

# NOTE: gazetteer.json, elevation.json and the scenario/metric JSONs are
# deliberately COMMITTED. They make the repo reproducible offline and let a
# reviewer run the app without waiting on Overpass rate limits.

# --- Credentials: never commit, even if a token file is placed here later ---
gh_token.txt
*token*.txt
*.pem
*.key
.env

# Competition problem statement — organisers' document, not ours to redistribute
AVINYA_2026_PS.pdf

# Conference poster template — organisers' document, same policy as the PS.
# build_poster.py expects it in the repo root; download it from the organisers.
1st_International_Conference_on_Sustainable_Innovation_and_Green.pptx

# PowerPoint PNG/PDF export scratch — regenerated on demand
drishti/poster/render/
drishti/deck/render/

# CarbonCampus (Round 1) poster lives in the carboncampus repo, not this one
poster_round1/
